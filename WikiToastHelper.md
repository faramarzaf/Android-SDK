```kotlin

      ToastHelper.Builder(this)
                .setMessage("text")
                .setTextColor("#color_code")
                .setTextSize(10)
                .setPadding(10)
                .setImageSize(64)
                .setIcon(R.drawable.ic)
                .setBackgroundColor(R.color.colorAccent)
                .setGravity(Gravity.CENTER)
                .build()
                
```
